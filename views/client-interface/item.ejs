<!DOCTYPE html>
<html lang="en">
  <head>
    <title>BookVerse</title>
    <link rel="icon" type="image/x-icon" href="/assets/logo/logo_favicon.png" />

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="BookVerse - Lebanon's top modern bookstore offering a wide range of books, Braille books, seasonal gifts, and B2B services. Discover the perfect book for any occasion!"
    />
    <meta
      name="keywords"
      content="BookVerse, Lebanese bookstore, books in Lebanon, Braille books, modern bookstore, B2B book services, seasonal book gifts, Beirut bookstore"
    />
    <meta name="author" content="BookVerse Team" />
    <meta name="theme-color" content="#ffffff" />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/client-styles.css" />
  </head>

  <body>
    <%- include('includes/header') %>

    <main>
      <div class="container">
        <section class="item-container container mt-5 mb-5">
          <div class="grid-layout">
            <img class="mb-1" src="<%= item.image %>" alt="<%= item.title %>" />

            <article class="product-article"><%= item.description %></article>

            <section class="second-container pt-5 pb-5">
              <div class="item-price pl-3 pr-3">
                <h1><%= item.title %></h1>
                <p style="color: gray;"><%= item.author %></p>

                <h6>
                  <% if (item.oldPrice && item.oldPrice > item.price) { %>
                  <del style="color: gray;"><%= item.oldPrice %>$</del>
                  <span style="color: red; margin-left: 10px;"><%= item.price %>$</span>
                  <% } else { %>
                  <span style="color: black;"><%= item.price %>$</span>
                  <% } %>
                </h6>
              </div>

              <form action="/cart/add" method="POST">
                <input type="hidden" name="bookId" value="<%= item.id %>" />
                <input type="hidden" name="quantity" value="1" />
                <button type="submit" class="mt-3 button-2 add-to-cart-button">
                  ADD TO CART
                </button>
              </form>
            </section>
          </div>
        </section>
      </div>
    </main>

    <%- include('includes/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
  </body>
</html>
